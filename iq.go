package main

import (
	nexusiq "github.com/sonatype-nexus-community/gonexus/iq"
)

func evaluateComponents(iq nexusiq.IQ, nexusApplication string, components []string) (map[string]string, error) {
	// remediation, err := nexusiq.GetRemediationByApp(iq, component Component, nexusiq.StageBuild, nexusApplication)
	return nil, nil
}
